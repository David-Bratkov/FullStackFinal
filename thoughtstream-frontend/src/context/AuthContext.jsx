/*

• Create a global React Context (e.g., AuthContext.jsx) to hold
   The token (JWT)
   Login and logout logic
   Utility functions like isAuthenticated
• Store the token in localStorage for persistence between reloads.
• Provide this context to the entire app using a top-level <AuthProvider>

*/